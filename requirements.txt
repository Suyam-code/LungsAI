# TensorFlow, chosen per platform:
#  - Linux (Streamlit Cloud): tensorflow-cpu skips ~2 GB of unused CUDA
#    libraries, so builds are much faster and stay inside size limits.
#  - macOS: tensorflow-cpu publishes no ARM wheels, so use plain tensorflow,
#    which has included Apple Silicon support since 2.16.
tensorflow-cpu>=2.16; sys_platform != 'darwin'
tensorflow>=2.16; sys_platform == 'darwin'

streamlit>=1.38
numpy>=1.24
pillow>=10.0

# Local Gradio entry point (app.py). Not needed by the Streamlit deployment.
gradio>=4.44

# Data-prep scripts and the optional FastAPI backend.
pandas>=2.0
matplotlib>=3.7
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
